import{AI_MAX_TOKENS_MIN,AI_TEMPERATURE,SYSTEM_PROMPT,SYSTEM_PROMPT_L2,SYSTEM_PROMPT_L3}from"../lib/config.js";import{createLogger}from"../lib/logger.js";import{AbstractProvider}from"./abstract-provider.js";const RETRY_CONFIG={maxRetries:2,baseDelayMs:1e3,maxDelayMs:5e3};async function fetchWithRetry(url,options){for(let i=0;i<=RETRY_CONFIG.maxRetries;i++)try{const response=await fetch(url,{...options,signal:AbortSignal.timeout(8e4)});if(!response.ok&&response.status>=500&&i<RETRY_CONFIG.maxRetries){const delay=Math.min(RETRY_CONFIG.baseDelayMs*2**i,RETRY_CONFIG.maxDelayMs);await new Promise(r=>setTimeout(r,delay));continue}return response}catch(error){if(error.name==="AbortError")throw new Error("Request timeout");if(error.name==="TypeError"&&error.message.includes("fetch")&&i<RETRY_CONFIG.maxRetries){const delay=Math.min(RETRY_CONFIG.baseDelayMs*2**i,RETRY_CONFIG.maxDelayMs);await new Promise(r=>setTimeout(r,delay));continue}throw error}}export const RESPONSE_SCHEMA={type:"json_schema",json_schema:{name:"grammar_correction",strict:!0,schema:{type:"object",properties:{corrected:{type:"string"},changes:{type:"array",items:{type:"object",properties:{original:{type:"string"},replacement:{type:"string"},explanation:{type:"string"}},required:["original","replacement","explanation"],additionalProperties:!1}},confidence:{type:"number"}},required:["corrected","changes"],additionalProperties:!1}}};export class AbstractOpenAICompatibleProvider extends AbstractProvider{constructor(apiKey,model){if(super(apiKey,model),new.target===AbstractOpenAICompatibleProvider)throw new Error("AbstractOpenAICompatibleProvider is abstract \u2014 extend it, do not instantiate directly");this.endpoint=""}async _callApi(text,systemPrompt,{useSchema=!0}={}){const log=createLogger(this.providerId),maxTokens=Math.max(text.length*3,AI_MAX_TOKENS_MIN),payload={model:this.model,messages:[{role:"system",content:systemPrompt},{role:"user",content:text}],temperature:AI_TEMPERATURE,max_tokens:maxTokens};useSchema&&(payload.response_format=RESPONSE_SCHEMA),log.info(`API request \u2192 ${this.endpoint}`,{model:this.model,inputLength:text.length,useSchema}),log.debug("Request payload:",payload),log.debug("API key: configured");const endTimer=log.time(`${this.providerId}-api-call`),response=await fetchWithRetry(this.endpoint,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${this.apiKey}`},body:JSON.stringify(payload)});if(!response.ok){endTimer();const err=await response.json().catch(()=>({})),customMsg=this._onApiError(response.status,err,response);throw customMsg?new Error(customMsg):(log.error(`API error ${response.status}:`,err),new Error(err.error?.message||`${this.providerName} API error: ${response.status}`))}const data=await response.json();endTimer(),log.group("API response",()=>{log.info(`Status: ${response.status}`),log.info(`Model used: ${data.model}`),data.usage&&log.info(`Tokens \u2014 prompt: ${data.usage.prompt_tokens}, completion: ${data.usage.completion_tokens}, total: ${data.usage.total_tokens}`)});const content=data.choices?.[0]?.message?.content;if(!content)throw log.error("Empty content in API response:",data),new Error(`Empty response from ${this.providerName}`);return{content,usage:data.usage||null}}async _doCorrectGrammar(text){const log=createLogger(this.providerId),attempt=async useSchema=>{const{content,usage}=await this._callApi(text,SYSTEM_PROMPT,{useSchema});log.debug("Raw response content:",content);try{const parsed=JSON.parse(content);return log.info(`Parsed result \u2014 ${parsed.changes?.length||0} corrections`),{...parsed,usage}}catch{throw log.error("JSON parse failed. Raw content:",content),new Error("Failed to parse grammar correction response")}};if(this._noStructuredOutput)return await attempt(!1);try{return await attempt(!0)}catch(err){if(err.message&&(err.message.includes("response_format")||err.message.includes("json_schema"))){log.warn("response_format rejected, retrying without it");try{const result=await attempt(!1);return this._noStructuredOutput=!0,log.info("Fallback succeeded \u2014 marked as noStructuredOutput"),result}catch(fallbackErr){throw log.error("Fallback also failed:",fallbackErr.message),err}}throw err}}async _doCorrectGrammarLevel2(text){const log=createLogger(this.providerId),{content,usage}=await this._callApi(text,SYSTEM_PROMPT_L2,{useSchema:!1});return log.debug("Raw L2 response content:",content),{...this._extractJsonFromText(content),usage}}async _doCorrectGrammarLevel3(text){const log=createLogger(this.providerId),{content}=await this._callApi(text,SYSTEM_PROMPT_L3,{useSchema:!1}),corrected=content.trim();return log.info(`L3 corrected text \u2014 ${corrected.length} chars`),{corrected,changes:[]}}_onApiError(_status,_err,_response){return null}}
